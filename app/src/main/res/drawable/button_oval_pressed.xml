<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners android:radius="@dimen/button_radius" />
    <solid android:color="@color/bg_btn" /> <!-- Màu nền của nút -->
    <stroke
        android:width="2dp"
        android:color="@android:color/holo_blue_light" /> <!-- Màu border khi đã nhấn -->
</shape>
